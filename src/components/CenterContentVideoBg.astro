---
const { content } = Astro.props;
import { getLangFromUrl } from "../i18n/utils";
import { pickLangPath } from "../utils/utils";

const lang = getLangFromUrl(Astro.url);
---

<section class="bg-my_gray-200 solution">
  <div class="flex items-center justify-center relative">
    <div
      class="flex flex-col h-full items-center justify-center p-10 relative what_are_we_doing"
    >
      <h3
        class="font-bold mb-2 mt-4 px-8 text-brown_dark-600 text-center text-lg uppercase"
      >
        {content.title}
      </h3>
      <p
        class="drop-shadow-lg font-light leading-relaxed mb-6 pb-10 pt-5 px-8 text-brown-800 text-xl"
      >
        {content.paragraph}
      </p>
      <div
        class="flex flex-col gap-0 gap-2 justify-evenly mb-10 w-full sm:flex-row"
      >
        <a
          href={`${pickLangPath(lang)}projects/tree-nursery`}
          class="bg-yellow_leave-400 font-light hover:bg-green-400 inline-block px-5 py-2 rounded-sm text-white text-xl uppercase"
          >{content.button}</a
        >
        <a
          href={`${pickLangPath(lang)}plant-and-protect`}
          class="bg-yellow_leave-400 font-light hover:bg-green-400 inline-block px-5 py-2 rounded-sm text-white text-xl uppercase"
          >{content.button_2}</a
        >
      </div>
    </div>
  </div>
</section>
