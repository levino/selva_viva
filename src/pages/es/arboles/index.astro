---
import TreesPage from "../../../pageLayouts/TreesPage.astro";
import { getCollection } from "astro:content";

const treesCollection = await getCollection("trees", ({ id }) => {
  return id.startsWith("es/");
});
---

<TreesPage treesCollection={treesCollection} />
