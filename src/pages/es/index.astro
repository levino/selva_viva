---
import FrontPage from "../../pageLayouts/FrontPage.astro";
import { getCollection } from "astro:content";
const [spanishContent] = await getCollection("frontPage", ({ slug }) => {
  return slug === "es";
});
console.log(spanishContent);
---

<FrontPage content={spanishContent} />
