---
import { getEntry } from "astro:content";
import FincaPageLayout from "../../../../pageLayouts/FincaPageLayout.astro";

const content = await getEntry("theFinca", "de");

const { data } = content;
---

<FincaPageLayout data={data} />
