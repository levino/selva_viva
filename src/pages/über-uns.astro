---
import AboutUsPage from "../pageLayouts/AboutUsPage.astro";
import { getCollection } from "astro:content";
const [pageContent] = await getCollection("aboutUsPage", ({ slug }) => {
  return slug === "de";
});
---

<AboutUsPage content={pageContent} />
