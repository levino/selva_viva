---
import FrontPage from "../pageLayouts/FrontPage.astro";
import { getCollection } from "astro:content";
const [deutschContent] = await getCollection("frontPage", ({ slug }) => {
  return slug === "de";
});
console.log(deutschContent);
---

<FrontPage content={deutschContent} />
